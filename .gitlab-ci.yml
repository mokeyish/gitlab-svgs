image: node:latest

stages:
  - previewbuilt

before_script:
  - npm install

cache:
  paths:
    - node_modules/

pages:
  stage: previewbuilt
  script:
    - echo "Starting SVG Built"
    - npm run svg
    - npm run generate
  artifacts:
    paths:
      - public
  only:
    - master

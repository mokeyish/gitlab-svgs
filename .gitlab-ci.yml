image: node:latest

before_script:
  - npm install

cache:
  paths:
    - node_modules/

pages:
  script:
    - npm run svg
    - npm run generate
  artifacts:
    paths:
      - public
  only:
    - master

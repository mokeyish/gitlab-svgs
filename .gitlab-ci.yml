image: node:latest

before_script:
  - npm install

cache:
  paths:
    - node_modules/

pages:
  script:
    - echo "Starting SVG Built"
    - npm run svg
    - npm run generate
  artifacts:
    paths:
      - public
  only:
    - master
